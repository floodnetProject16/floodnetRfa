% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AmaxData.R, R/DailyData.R, R/SequenceData.R,
%   R/StationData.R
\name{AmaxData}
\alias{AmaxData}
\alias{DailyData}
\alias{SequenceData}
\alias{StationData}
\title{Extract Annual, daily data from HYDAT}
\usage{
AmaxData(sites, db, target = NULL, distance = NULL, size = 25)

DailyData(
  sites,
  db,
  pad = FALSE,
  tol = 346,
  target = NULL,
  distance = NULL,
  size = 25
)

SequenceData(x, freq = "years", site.name = "site1", sdate = "1970-01-01")

StationData(sites, db)
}
\arguments{
\item{sites}{List of stations.}

\item{db}{HYDAT database.}

\item{distance}{A vector of distance between all sites.}

\item{size}{Size of the pooling group.}

\item{pad, tol}{Logical and number of days. Should the daily data be padded.
See \link[CSHShydRology]{PadPot}.}

\item{year}{Logical. Should the full date be return or only the year.}
}
\description{
Return a dataset of hydrometric data from HYDAT.
Optionally, a vector distance can be passed and so only a pooling groups is returned.
the target is the site with distance zero.
}
\examples{

\dontrun{
## This create a variable DB_HYDAT that point to the database
db <- DB_HYDAT

## Reading AMAX data for one station
x <- AmaxData(c('01AD002'), db)
head(x, 3)

## Reading multiple stations
x <- AmaxData(c('01AD002','01AF009'), db, year = FALSE)
x[seq(85,95),]

## Reading Daily data
x <- DailyData(c('01AD002','01AF009'), db)
head(x, 3)

## Filter the stations to keep only a pooling group of size 5
sid <- gaugedSites$supreg_km12 == 11
sites <- gaugedSites$station[sid]

coord <- gaugedSites[sid, c('lon','lat')]
rownames(coord) <- sites

h <- as.matrix(dist(coord))

x <- AmaxData(sites, db, target = '01AF009', size = 5,  distance = h)
table(x$station)
round(sort(h['01AF009',])[1:10],2)
}

}
\seealso{
\link{DailyPeaksData}.
}
